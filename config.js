/**
 * Configuration file
 *
 * This file contains the default configuration and can be safely committed.
 * For local development with different API keys, create a config.local.js file
 * which will be ignored by git.
 */

const config = {
  googleMaps: {
    // Default API key for development/demo purposes
    // In production, you should replace this with your restricted API key
    // Or create a config.local.js file to override this locally
    apiKey: 'AIzaSyC7xgv8UHqIezRn3vT0_c03x_5IxZxzu3I',
  },
};

export default config;
